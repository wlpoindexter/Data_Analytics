customer_id = 10482                    # Assumed integer; could be a string if it has leading zeros
customer_name = "Maria Lopez"          # First + last combined; maybe should be separate variables
customer_gender = "F"                  # Single char; could use full word or numeric code instead
customer_dob = "1992-04-15"            # String in ISO format; could use a date object instead
drivers_license = "D123-4567-8901"     # String; format varies by state
auto_policy_number = "POL-2024-00391"  # String; format set by insurer

my_name = "Will Poindexter"
hometown = "Chicago, Illinois"

# Full list of Python reserved words (keywords):
# False, None, True, and, as, assert, async, await, break, class, continue, def, del, elif, else, except, finally, for, from, global, if, import, in, is, lambda, nonlocal, not, or, pass, raise, return, try, while, with, yield

# if  – starts a conditional block; code inside runs only when the condition is True
# for – starts a ^loop^ that iterates over each item in a ^sequence^ (list, string, etc.)
# def – used to ^define^ (create) a reusable block of code called a ^function^
# return – exits a function and optionally sends a value back to the ^caller^
# import – brings an external ^module^ (library) into the current script so you can use it
